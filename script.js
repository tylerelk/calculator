const numbers = Array.from(document.querySelectorAll(".number-button")).map(x => x.innerText);
const operators = Array.from(document.querySelectorAll(".operation-button")).map(x => x.innerText);

let operatorSet = false;

numbers.addEventListener('click', () => {
    numbers.forEach
})